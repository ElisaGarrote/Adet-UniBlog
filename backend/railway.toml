[project]
name = "uniblog-backend"

[deploy]
start = "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"

[env]
DEBUG = "False"
CORS_ALLOW_ALL_ORIGINS = "False"
